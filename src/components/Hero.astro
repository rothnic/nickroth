---
import avatarPlaceholder from '../assets/images/avatar-placeholder.svg';
---

<section class="relative isolate overflow-hidden px-4 pt-32 pb-24 sm:px-6 sm:pt-40 lg:px-8">
  <div class="absolute left-1/2 top-16 h-[520px] w-[520px] -translate-x-1/2 rounded-full bg-primary/20 blur-3xl opacity-70"></div>
  <div class="absolute -right-24 bottom-0 h-[420px] w-[420px] rounded-full bg-accent/20 blur-3xl opacity-60 animate-pulse-slow"></div>
  <div class="absolute -left-32 bottom-20 h-[320px] w-[320px] rounded-full bg-secondary/20 blur-3xl opacity-40 animate-float"></div>

  <div class="mx-auto grid max-w-6xl items-center gap-16 lg:grid-cols-[1.1fr_0.9fr]">
    <div class="relative z-10 space-y-10">
      <span class="badge-pill text-primary/80">SYSTEMS-LED PRODUCT LEADERSHIP</span>
      <h1 class="text-balance text-4xl font-semibold leading-[1.05] text-base-content sm:text-5xl md:text-6xl">
        Compounding outcomes with AI-native product systems and relentless operations.
      </h1>
      <p class="max-w-xl text-lg text-base-content/70">
        I help growth-stage companies collapse the distance between insight, automation, and shipped value.
        From discovery rituals to AI-assisted delivery, every loop is engineered for speed and measurable momentum.
      </p>

      <div class="flex flex-wrap gap-4">
        <a href="/work" class="btn-gradient">
          Explore case studies
          <span aria-hidden="true">→</span>
        </a>
        <a href="/contact" class="btn-outline-ghost">
          Book a velocity session
        </a>
      </div>

      <div class="grid grid-cols-2 gap-4 sm:grid-cols-4">
        <div class="glass-stat">
          <p class="text-xs font-semibold uppercase tracking-[0.25em] text-base-content/50">Automation ROI</p>
          <p class="mt-2 text-2xl font-semibold text-primary">+$35K</p>
          <p class="text-xs text-base-content/50">monthly cost savings</p>
        </div>
        <div class="glass-stat">
          <p class="text-xs font-semibold uppercase tracking-[0.25em] text-base-content/50">Growth Velocity</p>
          <p class="mt-2 text-2xl font-semibold text-primary">150%</p>
          <p class="text-xs text-base-content/50">organic user lift</p>
        </div>
        <div class="glass-stat">
          <p class="text-xs font-semibold uppercase tracking-[0.25em] text-base-content/50">Revenue Impact</p>
          <p class="mt-2 text-2xl font-semibold text-primary">400%</p>
          <p class="text-xs text-base-content/50">YoY revenue increase</p>
        </div>
        <div class="glass-stat">
          <p class="text-xs font-semibold uppercase tracking-[0.25em] text-base-content/50">Reliability</p>
          <p class="mt-2 text-2xl font-semibold text-primary">95%</p>
          <p class="text-xs text-base-content/50">AI workflow accuracy</p>
        </div>
      </div>

      <div class="flex flex-wrap items-center gap-4 pt-2 text-sm text-base-content/60">
        <div class="h-2 w-16 overflow-hidden rounded-full bg-base-content/10">
          <div class="h-full w-full rounded-full bg-gradient-to-r from-primary via-accent to-secondary animate-gradient-x"></div>
        </div>
        <p>Currently designing autopilot loops for cross-functional product teams.</p>
      </div>
    </div>

    <div class="relative z-10">
      <div class="glass-panel relative overflow-hidden rounded-[32px] border border-base-content/10 p-8">
        <div class="flex items-center gap-4">
          <div class="relative">
            <span class="absolute -right-2 -top-2 rounded-full bg-success/20 px-2 py-1 text-[11px] font-semibold uppercase tracking-[0.3em] text-success">
              LIVE
            </span>
            <div class="h-16 w-16 overflow-hidden rounded-2xl border border-base-content/10 bg-base-100 shadow-lg">
              <img src={avatarPlaceholder.src} alt="Nick Roth" loading="lazy" class="h-full w-full object-cover" />
            </div>
          </div>
          <div>
            <p class="text-xs font-semibold uppercase tracking-[0.35em] text-base-content/50">Operating Guide</p>
            <p class="text-xl font-semibold text-base-content">Nick Roth</p>
            <p class="text-sm text-base-content/60">Product Systems &amp; AI Director</p>
          </div>
        </div>

        <div class="mt-8 grid gap-4">
          <div class="glass-tile">
            <div class="flex items-center justify-between text-xs uppercase tracking-[0.3em] text-base-content/50">
              <span>Signal Architecture</span>
              <span>Week 0</span>
            </div>
            <p class="mt-3 text-sm text-base-content/70">
              Merge research, analytics, and product intuition into one living source of truth that drives prioritization.
            </p>
          </div>
          <div class="glass-tile">
            <div class="flex items-center justify-between text-xs uppercase tracking-[0.3em] text-base-content/50">
              <span>Automation Fabric</span>
              <span>Weeks 1-3</span>
            </div>
            <p class="mt-3 text-sm text-base-content/70">
              Deploy AI-assisted workflows that eliminate repeatable toil while keeping humans in the loop for quality.
            </p>
          </div>
          <div class="glass-tile">
            <div class="flex items-center justify-between text-xs uppercase tracking-[0.3em] text-base-content/50">
              <span>Delivery Cadence</span>
              <span>Weekly</span>
            </div>
            <p class="mt-3 text-sm text-base-content/70">
              Instrument discovery-to-delivery rituals so every sprint ships value, insight, and learning back into the system.
            </p>
          </div>
        </div>

        <div class="mt-8 rounded-2xl border border-base-content/10 bg-base-content/5 p-5">
          <div class="flex items-center justify-between text-xs font-semibold uppercase tracking-[0.3em] text-base-content/50">
            <span>Momentum Snapshot</span>
            <span>90 Day Runway</span>
          </div>
          <div class="mt-4 flex flex-wrap items-end justify-between gap-6">
            <div class="flex-1 min-w-[180px]">
              <div class="h-2 overflow-hidden rounded-full bg-base-content/10">
                <div class="h-full w-[82%] rounded-full bg-gradient-to-r from-primary via-accent to-secondary animate-float-line"></div>
              </div>
              <p class="mt-3 text-xs text-base-content/60">
                Automation-first rituals returning 30+ hours per week to roadmap execution.
              </p>
            </div>
            <div class="text-right">
              <p class="text-3xl font-semibold text-primary">94%</p>
              <p class="text-xs uppercase tracking-[0.25em] text-base-content/50">cycle efficiency</p>
            </div>
          </div>
        </div>
      </div>
      <div class="pointer-events-none absolute -right-6 -top-10 h-32 w-32 rounded-full border border-primary/40 bg-gradient-to-br from-primary/25 to-transparent blur-xl animate-orbit"></div>
      <div class="pointer-events-none absolute -bottom-8 left-12 h-24 w-24 rounded-full border border-secondary/40 bg-gradient-to-br from-secondary/20 to-transparent blur-lg animate-float"></div>
    </div>
  </div>
</section>
