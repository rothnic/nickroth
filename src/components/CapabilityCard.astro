---
export interface Props {
  title: string;
  description: string;
  icon?: string | undefined;
}

const { title, description, icon = "🚀" } = Astro.props;
---

<article class="group relative overflow-hidden rounded-3xl border border-white/5 bg-base-100/70 p-6 backdrop-blur transition-all duration-300 hover:-translate-y-2 hover:border-primary/50 hover:shadow-[0_30px_60px_-40px_rgba(76,29,149,0.8)]">
  <div class="absolute -right-10 -top-10 h-32 w-32 rounded-full bg-gradient-to-br from-primary/30 via-secondary/20 to-transparent blur-3xl transition-opacity duration-300 group-hover:opacity-100 opacity-60"></div>
  <div class="relative flex flex-col gap-4">
    <span class="flex h-12 w-12 items-center justify-center rounded-2xl bg-gradient-to-br from-primary/80 via-secondary/70 to-accent/70 text-2xl shadow-lg shadow-primary/25">
      {icon}
    </span>
    <h3 class="text-xl font-semibold tracking-tight text-base-content">{title}</h3>
    <p class="text-sm leading-relaxed text-base-content/70">{description}</p>
    <div class="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.25rem] text-primary/70">
      <span class="h-px flex-1 bg-gradient-to-r from-transparent via-primary/40 to-primary/70"></span>
      Operating Layer
    </div>
  </div>
</article>