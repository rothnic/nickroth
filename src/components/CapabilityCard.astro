---
export interface Props {
  title: string;
  description: string;
  icon?: string | undefined;
}

const { title, description, icon = "🚀" } = Astro.props;
---

<!-- Clean RetroUI Card -->
<div class="border-2 border-black rounded-none shadow-[4px_4px_0px_0px_#000] bg-card text-card-foreground h-full w-full">
  <div class="p-6">
    <div class="text-4xl mb-4">{icon}</div>
    <h3 class="text-2xl font-semibold mb-4 text-foreground">{title}</h3>
    <p class="text-muted-foreground leading-relaxed">{description}</p>
  </div>
</div>