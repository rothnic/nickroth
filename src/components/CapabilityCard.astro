---
export interface Props {
  title: string;
  description: string;
  icon?: string | undefined;
}

const { title, description, icon = "🚀" } = Astro.props;
---

<!-- Pure RetroUI Card with Brutalist Styling -->
<div class="inline-block border-2 border-black rounded shadow-md transition-all hover:shadow-none hover:translate-y-1 bg-card h-full w-full duration-200">
  <div class="p-6">
    <div class="text-4xl mb-4">{icon}</div>
    <h3 class="text-2xl font-medium mb-4 text-foreground">{title}</h3>
    <p class="text-muted-foreground leading-relaxed">{description}</p>
  </div>
</div>