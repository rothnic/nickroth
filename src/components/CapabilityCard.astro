---
export interface Props {
  title: string;
  description: string;
  icon?: string | undefined;
}

const { title, description, icon = "🚀" } = Astro.props;
---

<article class="group relative overflow-hidden rounded-3xl border border-base-content/10 bg-base-100/80 p-8 transition-all duration-500 hover:-translate-y-3 hover:border-primary/40 hover:shadow-[0_40px_80px_-45px_rgba(15,23,42,0.55)]">
  <div class="absolute inset-0 opacity-0 transition-opacity duration-500 group-hover:opacity-100">
    <div class="absolute inset-0 bg-gradient-to-br from-primary/10 via-transparent to-secondary/15"></div>
  </div>
  <div class="relative flex h-full flex-col gap-6">
    <span class="inline-flex h-14 w-14 items-center justify-center rounded-2xl border border-base-content/10 bg-base-100 text-2xl shadow-sm transition-transform duration-500 group-hover:-rotate-3 group-hover:scale-105">
      {icon}
    </span>
    <div class="space-y-3">
      <h3 class="text-xl font-semibold text-base-content">{title}</h3>
      <p class="text-sm leading-relaxed text-base-content/70">{description}</p>
    </div>
    <div class="mt-auto flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.3em] text-base-content/40">
      <span class="inline-block h-2 w-2 rounded-full bg-primary"></span>
      <span>Plays well with operations, product, and engineering</span>
    </div>
  </div>
</article>
